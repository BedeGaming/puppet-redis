# Redis dynamic config for <%= @name %>
# Note, this file will be wiped by Puppet if the included file below is changed

include <%= @conf_path %>/redis.d/<%= @name %>_<%= @conf %>

# Anything below this line is managed by the Redis instance itself

